<% let stylesheets=["/css/profile.css"]; %>
<% let scripts=[{ script: "/js/common.js" }, { script: "/js/profile.js" }]; %>

<%- include("templates/header", { stylesheets: stylesheets }) %>
<div id="profilePage">
    <div class="profile-container">
        <h1>User Profile</h1>
        <form id="profileForm">
            <div class="form-group">
                <label for="fullName">Full Name:</label>
                <input type="text" id="profile-name" value="<%- locals?.name ? name : "" %>" disabled>
            </div>
            <div class="form-group">
                <label for="email">Email Address:</label>
                <input type="email" id="profile-email" value="<%- locals?.email ? email : "" %>" disabled>
            </div>
            <!-- <div class="form-group">
                <label for="address">Home Address:</label>
                <input type="text" id="address" value="1234 Main Street, Vancouver, BC" disabled>
            </div>
            <div class="form-group">
                <label for="alertType">Preferred Alert Type:</label>
                <select id="alertType" disabled>
                    <option selected>Flood</option>
                    <option>Heatwave</option>
                    <option>Wildfire</option>
                </select>
            </div>
            <div class="form-group">
                <label for="notification">Notification Preference:</label>
                <select id="notification" disabled>
                    <option selected>Email</option>
                    <option>SMS</option>
                    <option>None</option>
                </select>
            </div> -->
            <div class="button-group">
                <button type="button" id="editBtn">Edit</button>
                <button type="submit" id="saveBtn" disabled>Save</button>
                <button type="button" id="deleteBtn" class="danger">Delete</button>
            </div>
        </form>
    </div>
</div>
<%- include("templates/footer", { scripts: scripts }) %>